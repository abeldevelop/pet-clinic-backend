server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abeldevelop/pet-clinic-config-repository
          search-paths:
          - '{application}'